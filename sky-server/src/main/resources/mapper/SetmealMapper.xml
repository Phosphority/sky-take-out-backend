<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.SetmealMapper">
    <insert id="addSetmeal" parameterType="Setmeal" useGeneratedKeys="true" keyColumn="id">
        insert into setmeal(category_id, name, price, description, image, create_time, update_time, create_user, update_user)
        values (#{categoryId}, #{name}, #{price}, #{description}, #{image}, #{create_time}, #{update_time}, #{create_user},
                #{update_user})
    </insert>
    <delete id="deleteBatch">
        delete from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findDishByDishId" resultType="integer">
        select count(*) from setmeal_dish where dish_id in
        <foreach collection="dishIds" item="dishId" open="(" separator="," close=")">
            #{dishId}
        </foreach>
    </select>

    <select id="page" resultType="Setmeal">
        select * from setmeal
        <where>
            <if test="name != null"> name like concat('%',#{name},'%') </if>
            <if test="categoryId != null"> categoryId = #{categoryId}) </if>
            <if test="status != null"> status = #{status} </if>
        </where>
    </select>

    <select id="findBatchStatus" resultType="integer">
        select * from setmeal where status = 1 and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>